version: 2

build:
  image: latest
  os: ubuntu-20.04
  tools:
    python: mambaforge-4.10

conda:
  environment: docs/environment.yml

python:
   version: 3.9
   install:
      - method: pip
        path: .

submodules:                                                                                                                   
    include: all
    
sphinx:
  configuration: docs/source/conf.py

formats: []
